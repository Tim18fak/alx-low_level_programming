#include "main.h"

/**
 * * main - check the code for alx school student.
 *
 * Return: Always 0.
 */
int main (void)
{
print_square(2)
	print_squre(10);
		print_square(10);
			printf_squre(0);
			return (0);
			return (0)
}
