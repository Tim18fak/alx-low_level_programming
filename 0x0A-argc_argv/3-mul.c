#include <stdio.h>

/**
 * main - multiple numbers
 * a: any number
 * b: any other number
 * c: result
 *
 * Return: 0
 */

int main(void)
{
	int a;
	int b;
	int c = a * b;

	printf("the result of the two number is %d\n", c);

	return (0);
}
